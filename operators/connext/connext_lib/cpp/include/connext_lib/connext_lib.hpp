#pragma once

namespace connext_lib {

/**
 * Lightweight placeholder API for the Connext C++ helper library.
 *
 * Downstream consumers can use this function to verify the compiled
 * library links correctly before the full implementation is provided.
 */
int version();

}  // namespace connext_lib

