<?xml version="1.0" encoding="UTF-8"?>

<dds xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="http://community.rti.com/schema/7.3.0/rti_converter.xsd">

    <!--  A definition of a Converter instance to run -->
    <converter name="UserConverter">
        <!--  Input storage settings. These settings are integrated with the
              settings described in the configuration 'UserRecorderService' that
              can be found in file USER_RECORDING_SERVICE.xml -->
        <input_storage>
            <sqlite>
                <storage_format>XCDR_AUTO</storage_format>
                <database_dir>cdr_recording</database_dir>
            </sqlite>
        </input_storage>

        <!--  Output storage settings -->
        <output_storage>
            <sqlite>
                <storage_format>JSON_SQLITE</storage_format>
                <!-- We need to specify a different directory, it cannot be the
                     same as the input -->
                <fileset>
                    <workspace_dir>json_converted</workspace_dir>
                    <filename_expression>rti_recorder_default_converted.db</filename_expression>
                </fileset>
            </sqlite>
        </output_storage>

        <!--  Domain selection: assume 0 by default.
              Converter is not a DDS application, so this won't create a
              DDS Domain Participant, but allows you to select which domains
              to convert -->
        <domain_participant name="Domain0">
            <domain_id>0</domain_id>
        </domain_participant>

        <session name="DefaultSession"
                 default_participant_ref="Domain0">
            <!-- Topics to convert in this session -->
            <topic_group name="DefaultTopicGroup">
                <!-- Rules describing the topics to convert -->
                <allow_topic_name_filter>*</allow_topic_name_filter>
                <deny_topic_name_filter>rti/*</deny_topic_name_filter>
            </topic_group>
        </session>
    </converter>

    <converter name="UserConverterJson">
        <!--  Input storage settings. These settings are integrated with the
              settings described in the configuration 'UserRecorderServiceJson'
              that can be found in file USER_RECORDING_SERVICE.xml -->
        <input_storage>
            <sqlite>
                <storage_format>JSON_SQLITE</storage_format>
                <database_dir>json_recording</database_dir>
            </sqlite>
        </input_storage>

        <!--  Output storage settings -->
        <output_storage>
            <sqlite>
                <storage_format>XCDR_AUTO</storage_format>
                <!-- We need to specify a different directory, it cannot be the
                     same as the input -->
                <fileset>
                    <workspace_dir>cdr_converted</workspace_dir>
                    <filename_expression>rti_recorder_default_converted.db</filename_expression>
                </fileset>
            </sqlite>
        </output_storage>

        <!--  Domain selection: assume 0 by default.
              Converter is not a DDS application, so this won't create a
              DDS Domain Participant, but allows you to select which domains
              to convert -->
        <domain_participant name="Domain0">
            <domain_id>0</domain_id>
        </domain_participant>

        <session name="DefaultSession"
                 default_participant_ref="Domain0">
            <!-- Topics to convert in this session -->
            <topic_group name="DefaultTopicGroup">
                <!-- Rules describing the topics to convert -->
                <allow_topic_name_filter>*</allow_topic_name_filter>
                <deny_topic_name_filter>rti/*</deny_topic_name_filter>
            </topic_group>
        </session>
    </converter>
</dds>
